# AH-24 — Приватность как жёсткое ограничение dedup

**Task:** CUBR-0051 (parent CUBR-0047) · **Track:** Cubrim-2 Global Addresser · **Wave:** W1
**Stage:** formulation (research-loop step 1) · **Status:** OPEN · **Date:** 2026-07-13
**Author:** Addressor Research Lead (Fable 5, voice-bearing) · **Index:** ADDR-W1-hypotheses-formulation.md

## Каноническая формулировка (если X → Y на Z, потому что M)

Если dedup кросс-пользовательский, то hit/miss утекает информацию о чужих данных, поэтому на приватных классах допустима только convergent encryption с per-domain солью, которая убивает кросс-пользовательский dedup, потому что одинаковые открытые тексты перестают совпадать между доменами.

## Поля карточки

- **Физическая модель / каталог:** известный класс атак на облачную дедупликацию; соль разрывает конвергентность
- **Класс данных (Z):** приватные данные vs публичный контент
- **Address target:** не применим (схема не ссылочная / инфра-статья)
- **Predicted lever (предсказание, НЕ измерение):** privacy-safe адресатор ограничен публичным контентом + dedup внутри доверенного домена (predicted)
- **Ceiling-категория:** `infra-cost`
- **Falsification test:** модель режимов; существование режима с >=80% исходного hit-rate опровергло бы жёсткость ограничения
- **measured_result:** null — измерений не было, статус OPEN честен
- **code_ref:** null — кода ещё нет
- **Source:** dedup side-channel literature + Non-Harm

## total_cost = address + metadata + residual + amortized(matrix/catalog/distribution/sync/integrity)

цена приватности: потерянный кросс-доменный hit-rate под convergent encryption с per-domain солью; плюс риск side-channel (hit/miss утечка) без неё.

## Честный потолок / граница no-win

Не выигрывает: приватные классы при строгой соли — кросс-пользовательский dedup убит по построению; privacy-safe периметр = публичный контент + доверенный домен.

## Авторский разбор (из синтез-отчёта волны 1)

### AH-24 · Приватность как жёсткое ограничение dedup — `infra-cost`
**Если** dedup кросс-пользовательский, **то** сам факт hit/miss утекает информацию («этот файл у кого-то уже есть» — известный класс атак на облачную дедупликацию), поэтому на классе **приватных данных** допустима только convergent encryption с per-user/domain солью, которая УБИВАЕТ кросс-пользовательский dedup, **потому что** одинаковые открытые тексты перестают давать одинаковые шифротексты между доменами.
- Predicted: privacy-safe адресатор ограничен публичным/общим контентом + dedup внутри доверенного домена; глобальный hit-rate под privacy-режимом заметно ниже наивного.
- Falsification: модель режимов (публичные блобы конвергентно + приватные локально); если существует режим с ≥80% исходного hit-rate — ограничение мягче предсказанного.

## Deep-research verdict (2026-07-15)

DR-r-privacy-v1 — public/private-split сохраняет 100.0% дедупа на hub-and-spoke флоте; «жёсткое» ограничение опровергнуто для этой топологии, остаётся правилом для мульти-приватных. NO-GO(as-hard-constraint). Полные числа/sensitivity/фальсификация — в БД (measured_result) и на cubrim.com/addressor.
