# AH-18 — Router: адресатор + Cubrim-1 (competitive selection)

**Task:** CUBR-0051 (parent CUBR-0047) · **Track:** Cubrim-2 Global Addresser · **Wave:** W1
**Stage:** formulation (research-loop step 1) · **Status:** OPEN · **Date:** 2026-07-13
**Author:** Addressor Research Lead (Fable 5, voice-bearing) · **Index:** ADDR-W1-hypotheses-formulation.md

## Каноническая формулировка (если X → Y на Z, потому что M)

Если построить двухфазный кодек (фаза 1: whole-file/CDC exact-dedup по матрице; фаза 2: residual в локальный кодек Cubrim-1), то total_cost на реальных смесях будет <= min(каждого поодиночке)+ε и строго лучше на смесях с dup-массой >0, потому что рычаги ортогональны.

## Поля карточки

- **Физическая модель / каталог:** глобальная повторность и локальная статистика не конкурируют за одни байты; regression-proof по Gotcha #4 паттерну
- **Класс данных (Z):** реальные смешанные user-миксы
- **Address target:** не применим (схема не ссылочная / инфра-статья)
- **Predicted lever (предсказание, НЕ измерение):** строгое не-ухудшение + аддитивный выигрыш на dup-массе
- **Ceiling-категория:** `identity-dedup`
- **Falsification test:** порог dup-массы, ниже которого интеграционный оверхед съедает выигрыш
- **measured_result:** null — измерений не было, статус OPEN честен
- **code_ref:** null — кода ещё нет
- **Source:** Cubrim-1 Gotcha #4 competitive selection

## total_cost = address + metadata + residual + amortized(matrix/catalog/distribution/sync/integrity)

сумма фаз: dedup-статьи (AH-02/05) на покрытой массе + полный Cubrim-1 total_cost на residual + scheme-байты и границы фаз (интеграционный оверхед).

## Честный потолок / граница no-win

Не выигрывает (фаза 1): при dup-массе ниже измеримого порога — но по построению (competitive selection, Gotcha #4) router не хуже локального кодека.

## Авторский разбор (из синтез-отчёта волны 1)

### AH-18 · Router: адресатор ⊕ Cubrim-1 (competitive selection) — `identity-dedup`+`shared-context`
**Если** построить двухфазный кодек — фаза 1: exact-dedup по матрице (whole-file AH-05, затем CDC AH-02), фаза 2: residual → локальный кодек Cubrim-1, — **то** total_cost на классе **реальных смешанных user-миксов** будет ≤ min(каждый метод поодиночке) + ε и строго лучше обоих на смесях с ненулевой dup-массой, **потому что** рычаги ортогональны: глобальная повторность и локальная статистика не конкурируют за одни байты.
- Архитектурный паттерн — регрессия-стойкий competitive selection (Gotcha #4: encoder пишет min(new, baseline) + scheme-байт).
- Falsification: если интеграционный оверхед (манифест, границы фаз) съедает выигрыш при низкой dup-массе — измерить порог dup-массы, ниже которого фаза 1 выключается.

## Verdict — GO (2026-07-14, W1-router-v1, MEASURED)

Двухфазный router измерен на реальном миксе 8.79 ГиБ с пофайловым competitive selection (Gotcha #4): **router 0.4504 ≤ min(local 0.5535, addr-only 0.4528)**; с полностью заряженным каталогом 0.4533 — на 10.0 пп лучше локального. ε scheme-байтов 0.0023%. Маршрутизация: 96 278 файлов → адресатор (из них 95 557 whole-file hits), 107 567 → локальный. **Эмпирический порог включения фазы 1: ~10% пофайловой dup-fraction** (резкая ступень: 1.0% выбора addr в дециле 0–9% → 96.6–100% от 10% и выше). **GO.** Следующий шаг — подстановка реального бэкенда Cubrim-1 вместо zstd-3 (уровень сокращался из вердикта с обеих сторон).
