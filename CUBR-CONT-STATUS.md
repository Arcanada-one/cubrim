start H-20 order-2-rANS 2026-06-23T13:02:18Z
built H-20 charged probe (python, no rust needed for gate)
measured order-2-rANS: delta o2-o1 POSITIVE on all 10 files (+225 sparse_clustered ... +1144 text); aggregate stays 0.221726, zero per-file wins
built scheme-8 Order2Rans (rust compiles clean)
tests-green 190 passed incl order-2 round-trip+property+competitive on all 10 files
measured 0.207618 (champion 0.221726, -6.36% rel); scheme-8 wins binary_mixed 8205->6885, text 3177->2889, sparse_clustered 443->400; RT PASS all 10
VERDICT go H-20 order-2-rANS scheme-8: aggregate 0.221726 -> 0.207618 (-6.36% rel), RT clean 10/10, 190 tests green, committed 13c26aa on feat/cubr-h20-order2-rans (NOT pushed, leaderboard untouched)
STOP clean: GO committed-not-pushed, monitor assigns next hypothesis
start H-21 adaptive-streaming-rANS 2026-06-23T14:19:51Z
probe H-21 (FIXED order-1, range-coder-accurate w/ rescaling): adaptive order-1 NO transmitted tables wins big -- sparse_clustered vs 120, text vs 1719, log_like vs 482, block_bound 3433; projected agg 0.194 (cube-only, raw held)
built+tests-green scheme-9 BwtAdaptive (range coder), 190 passed incl rescale stress + corpus RT both entry points
measured 0.177122 vs champion 0.221726 (-20.12% rel); scheme-9 wins sparse_clustered 443->179, text 3177->1784, log_like 1402->570, binary_mixed 8205->6148 (raw->cube), block_bound_runs 4169->3495; RT PASS all 10
VERDICT go H-21 adaptive-range-coding scheme-9: aggregate 0.221726 -> 0.177122 (-20.12% rel), RT clean 10/10, 190 tests green, committed c1a9ce8 on feat/cubr-h21-adaptive-rans (NOT pushed, leaderboard untouched)
STOP clean: GO committed-not-pushed, monitor assigns next hypothesis
start H-22 context-mixing-o1+o0 2026-06-23T15:18:34Z
probe H-22: static JM-backoff NO-GO (order-0 prior misaligned w/ BWT contexts), but LEARNED-weight mix GO -- beats H-21 on 4/5 cube files (block_bound -711, binary_mixed -456, text -71); projected agg 0.16651 vs H-21 0.177122 vs champ 0.221726
built+tests-green scheme-10 BwtContextMix (range coder, pure+learned-mix modes), 190 passed incl f64-determinism rescale stress
measured 0.168262 vs champion 0.221726 (-24.11% rel) AND beats H-21 frontier 0.177122; scheme-10 wins all 5 cube files via learned-mix mode 1 (block_bound 4169->2950 beats gzip 3072, binary_mixed 8205->5679, text 3177->1757); RT PASS all 10
VERDICT go H-22 context-mixing scheme-10: aggregate 0.221726 -> 0.168262 (-24.11% rel), also beats H-21 frontier 0.177122; RT clean 10/10, 190 tests green, committed 42930fa on feat/cubr-h22-context-mixing (NOT pushed, leaderboard untouched)
NOTE: static order-0 backoff prior = documented NO-GO sub-result; learned-weight mix = GO
STOP clean: GO committed-not-pushed, monitor assigns next hypothesis
start H-23 interleaved/SIMD-rANS (throughput-only per brief) 2026-06-23T15:49:13Z
measured H-23: SIZE single=24387 -> 4way=24398 (+11 bytes, ratio-NEGATIVE); THROUGHPUT decode 4way 2.35x (502 vs 214 MB/s), 2way 1.68x; encode ~flat 1.05x
VERDICT nogo-throughput-only H-23 interleaved-rANS: ratio +11 bytes/file (NO-GO for ratio leaderboard), decode 2.35x faster (throughput-only, banked); committed 6dd7bb1 on feat/cubr-h23-interleaved-rans (NOT pushed, codec unchanged)
QUEUE EXHAUSTED: H-20 GO 0.2076, H-21 GO 0.1771, H-22 GO 0.1683, H-23 throughput-only NO-GO. All 4 brief hypotheses executed.
STOP clean.
start CONSOLIDATE-8-9-10 (port scheme-9 BwtAdaptive + scheme-10 BwtContextMix onto main which has scheme-8 Order2Rans) 2026-06-23T16:07:19Z
built consolidated codec: schemes 8+9+10 coexist behind unified competitive min() rail (BwtRans/BwtEntropy/EntropyContext/Order2Rans/BwtAdaptive/BwtContextMix), each keeps its own header byte (8,9,10); no name collisions (H-21 RangeEncoder/RC_*/AdaptModel vs H-22 CmRangeEncoder/CM_*/CmCtx are distinct)
tests-green 192 passed (incl 8 H-21 range-coder + 8 H-22 context-mix tests), RT PASS 10/10 corpus
measured combined aggregate 0.168227 via run_bench --value-scheme bwt-rans (competitive picks best per-file): sparse_clustered 179 (sch9), text 1757 (sch10), log_like 570 (sch9), binary_mixed 5679 (sch10), block_bound_runs 2950 (sch10); RT PASS all 10
VERDICT consolidate-8-9-10 OK: combined aggregate 0.168227 <= threshold 0.168262 (4 bytes better than H-22 standalone 0.168262, per-file min across schemes 9+10), RT clean 10/10, 192 tests green
FINAL AGGREGATE 0.168227
start H-24 logistic/geometric o2+o1+o0 mix (scheme-11) 2026-06-23T16:56:07Z
probe H-24: geometric mix of o2,o1,o0 beats current linear o1+o0 mix on ALL 5 cube files; quantized geo3Q SUM(cube) 9502.5 vs mix 10640.2 (-1138 ideal B); per-file wins text -238, binary_mixed -265, block_bound -515, log_like -92, sparse_clustered -7
projected aggregate 0.168227 -> ~0.1594 (clears champion decisively, edges gzip 0.159674)
VERDICT-PROBE go: implement scheme-11 BwtGeoMix (geometric/logistic learned-weight mix of order-2/1/0, regression-proof competitive min)
built scheme-11 BwtGeoMix (geometric/logistic o2+o1+o0 mix, range coder, ln-table); rust compiles clean, clippy adds 0 new warnings
tests-green 200 passed (+10 H-24: scheme-byte/unit-grid/high-entropy-rescale/corpus-RT/never-regress/property/truncated) incl f64-determinism stress; RT PASS 10/10 corpus + CLI
measured 0.158273 vs champion 0.168227 (-5.91% rel) AND BEATS gzip 0.159674 (-0.88% rel, 164 B); scheme-11 wins all 5 cube files: text 1757->1525, binary_mixed 5679->5330, block_bound 2950->2389 (beats gzip 3072 by 683), log_like 570->557, sparse_clustered 179->169; RT PASS all 10
added code_sha capture to run_bench.py gather_env (CLAUDE.md mandate gap fix)
VERDICT go H-24 geometric/logistic-mix scheme-11 BwtGeoMix: aggregate 0.168227 -> 0.158273 (-5.91% rel), ALSO BEATS gzip 0.159674 (-0.88% rel, 164 B) — GOAL REACHED; RT clean 10/10, 200 tests green, committed 3937caf (code) + 48e28b7 (bench) on feat/cubr-h24-logistic-mix (branched origin/main, NOT pushed, leaderboard untouched)
STOP clean: GO committed-not-pushed, gzip beaten, monitor assigns next hypothesis
start ROBUSTNESS-STUDY (holdout corpus: does gzip-parity generalise beyond the 10-file tuned leaderboard?) 2026-06-23T17:52:09Z
built SEPARATE diverse holdout corpus (6 real files, frozen+committed, DISJOINT from leaderboard corpus): rust_src.rs 26805 (huffman.rs), c_header.h 34649 (/usr/include/stdio.h), config.json 66294 (real WebStorm 3rd-party-libs JSON), prose.txt 17774 (man gzip English), data.csv 17029 (real financial CSV), exe.bin 39384 (/bin/cat ELF)
measured Cubrim (compress --value-scheme bwt-rans, competitive rail) AND bwt-geomix (champion scheme) vs gzip-9 vs zstd-19; RT PASS 6/6 both schemes; code_sha 48e28b7-dirty
NOTE bwt-rans and bwt-geomix produced BYTE-IDENTICAL output on ALL 6 holdout files — geomix's tuned-corpus win did not reproduce on a single holdout file (competitive rail selected the same per-file scheme)
per-file Cubrim-vs-gzip ratio: rust_src 0.2607 vs 0.2528 LOSS; c_header 0.2067 vs 0.1980 LOSS; config.json 1.0002 vs 0.1333 CATASTROPHIC-LOSS; prose 0.3713 vs 0.3762 WIN(marginal 1.3%); csv 0.2137 vs 0.2400 WIN(11%); exe 0.3705 vs 0.3653 LOSS(marginal)
AGGREGATE Cubrim 0.5214 vs gzip 0.2359 vs zstd 0.2214 — Cubrim 2.2x WORSE than gzip on holdout (vs +0.9% edge on tuned corpus); loses to zstd-19 on ALL 6 files
ROOT-CAUSE config.json catastrophe: 66294 B > b*b = 65536 (square-limit, use_square_limit=true) AND BWT primary index is u16 (<=65536) -> any input >64KB cannot enter cube/BWT mode, falls to raw-store = ZERO compression; confirmed boundary: 65000 B -> 0.134 cube mode, 66000 B -> 1.0002 raw
AGGREGATE-excl-oversized-file (5 files <=64KB) Cubrim 0.2874 vs gzip 0.2860 vs zstd 0.2681 — rough gzip PARITY holds within the 64KB cube envelope but Cubrim is NEVER ahead of gzip in aggregate and loses to zstd on every file
VERDICT robustness FAIL: the 2-byte gzip edge on the tuned 10-file corpus does NOT generalise. (1) hard 64KB architectural ceiling (u16 BWT index + square-limit) raw-stores any larger real file = unusable as a general archiver; (2) even within envelope Cubrim only reaches gzip parity, wins 2/6 files (csv, prose-marginal), loses to zstd-19 6/6. Champion is corpus-overfit, not a robust gzip-beater.
artefacts code/bench/gen_holdout_corpus.py + run_holdout_bench.py + documentation/ephemeral/research/h-robust-bench.json + frozen holdout/ corpus; committed feat/cubr-robustness (NOT pushed)
STOP clean: robustness study complete, honest FAIL reported, leaderboard untouched
start BIGFILES raise hard 64KB ceiling (u16 BWT index + square-limit, root-cause line 49/51) -- chunk arm 2026-06-23T19:20:00Z
built MODE_CHUNKED=2 container: encode_chunked slices >cube_size_limit input into min(b*b,65536)-byte blocks, each encoded independently (cube/BWT/raw) + framed [MAGIC][VER][2][n_blocks u32] then n*([sub_len u32][sub_blob]); decode detects mode 2 before header-parse, decodes+concats (fail-closed); primary_index STAYS u16 (every block <=65536 => primary<l<=65536<=u16::MAX, no overflow, no BWT wire change); inputs <=ceiling byte-identical to v1
measured >64KB NOW COMPRESSES (not raw-store, mode byte=2=chunked): 500KB text default-scheme 513225->321360 ratio 0.626158 RT byte-exact; geomix-through-chunks 20KB b=64(4096B blocks,5 chunks) 20000->2010 ratio 0.100500 RT byte-exact
tests-green cargo build --release clean; cargo test GREEN: lib 203 passed (200 baseline +3 net new: rename test_raw_store_for_large_input->test_chunked_container_for_large_input, +test_chunked_large_compressible_round_trips_and_shrinks +test_chunked_round_trip_various_sizes +test_chunked_bwt_family_round_trips) + integration 14 passed (differential 2, cubr0027/0028/0031 1+1+1, traceability 10); 0 failed across all binaries
NOTE pre-existing O(n^2) BWT suffix-sort: single 65536-byte geomix block >2min even release (chunking INHERITS not worsens; original codec paid same cost for any 65536-byte BWT input); default scheme fast on big files; future SA-IS swap to make geomix usable on big blocks
VERDICT done BIGFILES: hard 64KB ceiling REMOVED via MODE_CHUNKED container; >64KB files compress (mode=2, not raw-store) + round-trip byte-exact (500KB text 0.626158, geomix-multichunk 0.1005); cargo build+test green; chunk arm of 'widen u16->u32 and/or chunk' (u16 primary_index kept, no wire change); committed on feat/cubr-bigfiles (NOT pushed)
STOP clean: bigfiles done, committed-not-pushed
start SA-IS swap O(n^2) BWT suffix-sort -> linear SA-IS (goal: 65536 geomix block seconds not >2min) 2026-06-23T20:03:33Z
built SA-IS suffix array on doubled stream (seq.seq+sentinel), Nong-Zhang-Chan induced sort; rotation order = SA entries in [0,n); bwt_out VALUES identical to naive (tie-group invariant); exact primary recovered via Z-function (rotation0 last in SA tie-group => primary = pos0-(group_size-1)); LF-decode + u16 primary_index wire UNCHANGED
tests-green SA-IS byte-identical to naive proven: test_sais_bwt_matches_naive (10 fixed incl all-same/periodic + 2000 random small-alphabet + periodic 1500-blocks; bwt_out AND primary AND LF round-trip); full cargo test GREEN lib 204 (+1 equivalence) + integration 14, 0 failed, suite 30s (was 94s)
measured TIMING release: single 65536-byte geomix block 0.37s (WAS >2min) 65536->507 RT byte-exact; 500KB real text geomix-through-chunks 2.92s (WAS >2min timeout) mode=2 513225->4126 ratio 0.008039 RT byte-exact
VERDICT done SA-IS: linear suffix-array replaces O(n^2) rotation sort, full 65536 geomix block sorts in <0.4s, wire format byte-identical (same primary_index u16 + same LF-decode, equivalence-tested), cargo build+test green; committed on feat/cubr-bigfiles (NOT pushed)
NEXT-HYPOTHESIS check: CUBR-CONT-BRIEF.md queue (1 order-2-rANS=H-20 GO, 2 adaptive-rANS=H-21 GO, 3 context-mixing=H-22 GO, 4 interleaving=H-23 NO-GO-throughput) ALL DONE; +H-24 geomix GO (beats gzip), +consolidation, +robustness FAIL, +bigfiles ceiling, +SA-IS. QUEUE EXHAUSTED — no untried brief hypothesis remains.
RECOMMEND next (highest-value, enabled by this fix not a new ratio micro-hyp): re-run robustness holdout bench (run_holdout_bench.py) now that >64KB files compress — config.json 66294B previously raw-stored ratio 1.0002 (robustness root-cause line 49) now chunks+compresses; the honest holdout aggregate should be re-measured before any further corpus-tuned hypothesis.
STOP clean: bigfiles+SA-IS done (commits 1ecd0c6 + 255fe5c on feat/cubr-bigfiles, NOT pushed); brief queue exhausted, operator/monitor to assign new hypothesis or approve holdout re-measure.
start HOLDOUT-RECHECK re-run robustness bench post-bigfiles+SA-IS (does config.json now compress, does gzip-parity generalise?) 2026-06-23T20:49:13Z
measured holdout 6-file (run_holdout_bench.py, release bin code_sha e6f4462-dirty, RT PASS 6/6 BOTH bwt-rans+bwt-geomix): config.json FIXED 66294->9278 ratio 0.1400 (WAS raw-store 66308 ratio 1.0002) -- chunking removed the catastrophe
per-file Cubrim(bwt-rans) vs gzip-9: rust_src 6987 vs 6775 LOSS+212; c_header 7161 vs 6862 LOSS+299; config 9278 vs 8839 LOSS+439; prose 6600 vs 6687 WIN-87(1.3%); csv 3639 vs 4087 WIN-448(11%); exe 14590 vs 14388 LOSS+202
AGGREGATE holdout Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd-19 0.2214 (44701B); input 201935B; Cubrim +617B (+1.30% rel) BEHIND gzip, +3554B (+7.95% rel) behind zstd; wins vs gzip 2/6, loses 4/6; loses to zstd 6/6; bwt-rans==bwt-geomix bytes on ALL 6 (competitive rail same per-file pick, geomix tuned-corpus edge does NOT reproduce on holdout)
VERDICT holdout-recheck: ceiling fix is DECISIVE (aggregate 0.5214 -> 0.2390, catastrophe gone) BUT gzip-parity does NOT generalise to a WIN -- Cubrim is ~1.3% BEHIND gzip in aggregate on unseen data (parity-but-behind, not a gzip-beater) and loses to zstd-19 on every file. Honest answer: the tuned-corpus 2-byte edge generalises only to near-gzip-parity, never to an actual holdout win. NOT tuned to holdout (no config changed).
artefact documentation/ephemeral/research/h-robust2-bench.json written
PROPOSE-NEW-HYPOTHESIS H-25 (queue exhausted): LZ77 match modeling as a NEW non-BWT ValueScheme. Rationale: holdout shows the entire gap to gzip/zstd is LZ dictionary matching (long-range repeats) which Cubrim's cube+BWT+rANS pipeline has ZERO model for. NOT closed (closed=distance-map/N-sweep/RLE-prepass/external-address/inter-file-dedup per Gotcha#7/H-18; intra-file LZ is none of these). Design: tokenize (literal,match) via LZ77; literals -> Cubrim BWT+order-1 rANS backend; match length/distance streams -> rANS. 2026-06-23T20:51:31Z
PROBE H-25 (documentation/ephemeral/research/probe_h25_lz_match.py, charged cheap probe, NO Rust, NO holdout tuning): greedy LZ77 hash-chain 32KB window min-match-3; literal stream charged at order-1 entropy (the order-1 probe applies to the literal coder); matches charged two ways
PROBE-RESULT match coverage 84-96% of bytes on all 6 holdout files (massive LZ-exploitable repeats Cubrim ignores); literal H1 ~3.9-4.5 bits << H0 ~5.7-7.4 (order-1 literal coder helps a lot)
PROBE-RESULT charged sizes: est_real (naive deflate-like match codes 5+log2 base) total 54819B = +15% ABOVE gzip 47638B (loses 6/6); est_opt (info floor: matches at log2(dist)+log2(len), literals at H1) total 36192B = -24% BELOW gzip (beats gzip 6/6, would challenge zstd 44701B)
VERDICT-PROBE H-25 GO-to-implement (conditional): the information floor is ~24% under gzip on UNSEEN data with the whole question being match-stream entropy coding -- exactly Cubrim's rANS strength. BUT not free: a weak match coder loses to gzip (est_real +15%), so the crux is entropy-coding the DISTANCE stream (dominant cost, up to 15 bits/match). Worth real Rust as scheme-12 LzRans; regression-proof via competitive min() rail. One-hypothesis-per-run: implementation is the NEXT run.
STOP clean: holdout re-check done (committed c01d048, gzip-parity does NOT generalise to a win, honest FAIL-vs-gzip), H-25 LZ class proposed + cheap-probed GO-to-implement; NOT pushed; monitor/operator to greenlight H-25 Rust.
start H-25 LzRans IMPLEMENT (greenlit): ValueScheme byte 12, LZ77 tokenize -> literals via BWT+order1-rANS, len/dist via bit-length-bucket rANS + raw extra bits, behind competitive min() rail 2026-06-23T22:04:02Z
built scheme-12 LzRans (codec.rs lz_rans_encode/decode + SA-IS-shared bwt_rans literal backend + LzBitWriter/Reader; config.rs enum+byte 12; main.rs --value-scheme lz-rans); added to competitive rail + estimate_cube_size (Gotcha #4 regression-proof)
tests-green cargo build --release clean; cargo test GREEN lib 209 (+5 LzRans: scheme-byte, direct codes round-trip 200+ incl periodic/all-same/overlap, full-codec RT, competitive-non-regress, truncated-no-panic) + integration 14, 0 failed
measured TUNED corpus (run_bench --value-scheme bwt-rans, RT PASS 10/10): aggregate 0.158273 UNCHANGED vs H-24 champion -- LzRans never selected, per-file bytes identical, ZERO regression
measured HOLDOUT (run_holdout_bench, RT PASS 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd 0.2214 (44701B) -- BYTE-IDENTICAL to pre-LzRans h-robust2; LzRans selected on 0/6 holdout files
ROOT-CAUSE (measured, not guessed): LZ IS finding matches (diag: 519 matches incl len-9999, coverage high) so not a matching bug. Two overheads kill it: (1) raw DISTANCE extra-bits dominate (~900B raw bits for 519 matches on 12000-sym input) -- the probe's flagged crux, bucket+rawbits leaves within-bucket entropy uncoded; (2) literal substream pays FULL order-1 rANS table cost on the smaller literal stream (alpha=64: lz standalone 14064 vs best_other 6369, 2x WORSE). Probe est_opt floor (-24%) charged NEITHER; real coder lands ~est_real (+15% over gzip), exactly as the probe warned.
VERDICT nogo-ratio H-25 LzRans: does NOT beat gzip on unseen data (holdout byte-identical with/without it, never selected 0/6), does NOT improve tuned corpus (0.158273 unchanged). Implemented+wired+regression-proof+RT-clean (committed scaffold), but the probe's -24% info-floor is NOT realized: bucket+raw-extra distance coding + heavy order-1 literal backend > match savings. To realize the floor a FUTURE run needs full-resolution context-entropy distance coding (zstd-style FSE/rANS-with-context) + a lighter literal coder. NOT tuned to holdout.
artefacts h25-tuned-bench.json + h25-holdout-bench.json
start H-25b LzRans STRENGTHEN (reopen H-25): fix the 2 measured overheads -- (1) distance/len bucket+raw -> full-value byte-split order-0 rANS (code within-bucket bits, exploit repeated distances); (2) literals BWT+order1 -> order-0 rANS (lighter, no per-context table blowup) 2026-06-23T22:55:48Z
built H-25b: added rans_order0_encode/decode (single global table); lz_rans rewired -- literals order-0 rANS, len/dist split low/high byte each order-0 rANS (no raw bits, no LzBitWriter), match-len capped u16; flags stay order-1; same competitive rail + scheme byte 12
tests-green cargo build --release clean; cargo test GREEN lib 209 + integration 14, 0 failed; round-trip byte-exact (5 LzRans tests incl 200+ direct cases, full-codec, holdout 6/6)
measured FIXES WORKED (standalone LZ vs bwt-rans on holdout): exe.bin LZ 16619 vs bwt-rans 33042 (2x BETTER; was 2x WORSE in H-25 -- order-0 literal fix decisive on large alphabet); config.json-blk 10866 vs 15029; prose 8034 vs 10700; rust_src 8417 vs 12392; LZ now BEATS order-1/order-2 rANS + Huffman on 4/5 files
measured STILL LOSES to champion family (standalone LZ vs geomix winner): config 10866 vs 8668 (+25%), csv 5730 vs 3539 (+62%), prose 8034 vs 6468 (+24%), rust 8417 vs 6853 (+23%), exe 16619 vs 14294 (+16%) -- LZ never the per-file min, selected 0/6
measured HOLDOUT (run_holdout_bench, RT PASS 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd 0.2214 (44701B) -- BYTE-IDENTICAL to H-25/h-robust2; aggregate UNCHANGED, LzRans selected 0/6
VERDICT nogo-ratio H-25b: does NOT beat gzip on unseen data (standalone LZ config 11556 vs gzip 8839, csv 5730 vs 4087 -- loses 6/6; holdout aggregate unchanged 0.2390 > gzip 0.2359). The two fixes are REAL (LZ graduated from dead-weight/2x-worse to competitive mid-tier, beating 4 of 7 sibling schemes) but insufficient: on <=64KB cube blocks BWT+geometric-context-mixing (geomix) already extracts the local redundancy, and the residual literals coded order-0 leave order-1+ structure that geomix captures. distance byte-split helps structured files (csv repeated distances) but geomix still wins. Probe -24% floor NOT reached. NOT tuned to holdout.
artefact documentation/ephemeral/research/h25b-holdout-bench.json
start H-25c LzRans (reopen condition): (1) zstd-style repeat-offset distance cache (3-deep move-to-front; mode 0/1/2=reuse recent offset ~2bits, 3=new byte-split); (2) lighter literal coder = min(order-0, order-1 fallback-table rANS) no BWT doubling 2026-06-23T23:43:39Z
built H-25c: lz_rans rewired -- dist_modes order-1 rANS alphabet 4 + only NEW distances byte-split; literals pick min(order0,order1) with 1-byte lit_mode flag; LZ_REP_INIT=[1,4,8]; same competitive rail + scheme byte 12
tests-green cargo build --release clean; cargo test GREEN lib 210 (+1 new: LZ-WINS-on-long-range asserts scheme byte 12 + dispatch round-trip) + integration 14, 0 failed; round-trip byte-exact
measured MECHANISM WORKS -- on a within-block long-range input (10KB unit x6 = 60KB, one block): LZ=6867 is the SMALLEST Cubrim scheme (geomix 9628, rans 10311, adaptive 9504) -- LZ WINS the rail (-29% vs geomix), first time ever; near gzip 6404, zstd 5205. Repeat-offset lever confirmed: inter-copy distances collapse to mode-0.
measured HOLDOUT (run_holdout_bench, RT 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd 0.2214 (44701B) -- UNCHANGED, LzRans selected 0/6. Holdout files are single <=64KB blocks where geomix local context dominates and within-block long-range structure is thin.
measured ARCHITECTURAL CAP exposed -- 120KB 12-copy (2 chunked 64KB blocks): cubrim lz-rans 13871 vs gzip 6950 (2x WORSE) because the 64KB MODE_CHUNKED boundary forces re-coding the repeated unit per block, while gzip windows the whole file. Concatenated holdout (202KB, 4 blocks): cubrim 49106 vs gzip 48342 (still loses) -- cross-file long-range repeats LOST at block boundaries.
VERDICT nogo-ratio H-25c: does NOT beat gzip on unseen data (holdout 0.2390 > gzip 0.2359 UNCHANGED, LZ 0/6; concatenated 49106 > gzip 48342; 120KB-multicopy 13871 vs gzip 6950). The repeat-offset lever is REAL and CORRECT -- LzRans now WINS the rail on single-block long-range data (-29% vs geomix) -- but two ceilings remain: (a) holdout files lack within-block long-range structure, geomix wins; (b) Cubrim's 64KB chunk boundary caps LZ reach, so cross-block long-range (exactly where gzip/zstd window-match) is unreachable. Beating gzip on long-range needs a WHOLE-FILE LZ pre-pass BEFORE chunking -- an architecture change, not a value-scheme. NOT tuned to holdout.
artefact documentation/ephemeral/research/h25c-holdout-bench.json
start H-25d ARCHITECTURE reopen: whole-file LZ pre-pass BEFORE 64KB chunking as a NEW CONTAINER MODE (MODE_LZ=3, not a value-scheme), so cross-block long-range repeats become reachable; behind competitive size pick (emit min(base, lz) + mode byte) = structurally regression-proof 2026-06-23T23:59:05Z
built H-25d: encode_with_config now = min(encode_base, encode_lz_prepass) for inputs >cube_size_limit; encode_lz_prepass LZ77-tokenizes whole file, literal residue -> encode_base (nested chunked cube/BWT/rANS pipeline), match streams via shared lz_encode_token_streams (repeat-offset cache, 4-byte distance split for >64KB distances); MODE_LZ decode mirrors; refactored lz_rans to share token-stream helpers
tests-green cargo build --release clean; cargo test GREEN lib 213 (+3 MODE_LZ: cross-block-win, no-regression-on-incompressible, multi-size RT) + integration 14, 0 failed; round-trip byte-exact; tuned corpus UNCHANGED (all <=64KB -> no prepass -> byte-identical, ZERO regression)
measured 120KB-MULTICOPY (12x 10KB unit, cross-block repeats at dist 10KB): cubrim 6934 (mode=3 MODE_LZ, RT PASS) vs gzip 6950 vs zstd 5202 -- CUBRIM BEATS GZIP (-16B, first gzip-win in the entire H-25 line); 2x better than H-25c 13871. Whole-file LZ captured the cross-block repeats the 64KB chunk boundary previously destroyed.
measured CONCATENATED holdout (202KB, 6 DIVERSE files): cubrim 49106 (mode=2 MODE_CHUNKED -- MODE_LZ fell back, no cross-FILE repeats among different file types) vs gzip 48342 vs zstd 44338 -- still loses to gzip; diverse files do not repeat each other so whole-file LZ finds nothing to exploit
measured HOLDOUT (run_holdout_bench, RT 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd 0.2214 (44701B) -- UNCHANGED, MODE_LZ selected 0/6 (config.json 66294B is 2 blocks but has no cross-block repeats; within-block redundancy already captured by chunked geomix)
VERDICT mixed H-25d: the ARCHITECTURE LEVER WORKS and is REGRESSION-PROOF -- whole-file LZ makes Cubrim BEAT GZIP on genuine cross-block long-range data (120KB multi-copy 6934 < gzip 6950, FIRST gzip-win in H-25), 2x better than chunked-only. BUT it does NOT help unseen REAL data: the holdout files lack cross-block repeats (unchanged 0.2390 > gzip 0.2359, MODE_LZ 0/6) and the concatenated DIVERSE holdout has no cross-file repeats (falls back to chunked, 49106 > gzip 48342). zstd-19 still leads everywhere. So whole-file LZ beats gzip ONLY when the data genuinely has long-range/duplicate structure; on the diverse holdout it does not. NOT tuned to holdout.
artefact documentation/ephemeral/research/h25d-holdout-bench.json
start H-25e zstd-class back-end on LZ match streams (reopen): FSE/rANS-with-context on offset/match-len/literal-len + lazy/optimal parse to close the zstd gap on long-range 2026-06-24T00:16:46Z
diagnosed (debug breakdown) the real lever: on 120KB-multicopy MODE_LZ total 6934 = lit_blob 1322 + token_streams 5590 (n_matches=2424!) -- GREEDY LZ took thousands of length-3 matches at random FAR offsets, each ~2.3B replacing ~1.5B of literals. Not an entropy problem -- a PARSE problem.
built H-25e: cost-aware + lazy LZ parse -- a match is taken ONLY when est coded cost (repeat-offset-aware offset bits + length bits + flag) < bytes-as-literals (per-literal cost = clamped H0 of the input); 1-step lazy lookahead prefers a strictly-longer match. Match streams already order-0 rANS (= FSE-equivalent); shared by value-scheme + MODE_LZ container.
tests-green cargo build --release clean; cargo test GREEN lib 213 + integration 14, 0 failed; round-trip byte-exact; tuned corpus UNCHANGED (regression-proof via competitive rail + mode byte)
measured 120KB-MULTICOPY: cost-aware parse cut matches 2424 -> 16, token_streams 5590 -> 210; cubrim 6934 -> 5359 (mode=3 MODE_LZ, RT PASS) vs gzip 6950 vs zstd 5202 -- BEATS gzip by 23% AND NARROWS to zstd from 33% behind (H-25d) to 3% behind (157B). Literals now dominate (5127 ~ zstd's literal cost).
measured CONCATENATED holdout 202KB (6 diverse files): cubrim 49106 (mode=2 MODE_CHUNKED -- MODE_LZ still falls back, diverse files share no cross-file repeats) vs gzip 48342 vs zstd 44338 -- UNCHANGED, still loses
measured HOLDOUT (run_holdout_bench, RT 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd 0.2214 (44701B) -- UNCHANGED, MODE_LZ 0/6; holdout files have no cross-block structure so MODE_LZ is never selected
VERDICT partial-win H-25e: the zstd-class lever that MATTERED was the OPTIMAL/LAZY PARSE (zstd's actual win), not FSE-vs-rANS (rANS already = FSE; match streams are tiny once the parse is fixed). On genuine long-range data it NARROWS the gap to zstd decisively -- 120KB 5359 vs zstd 5202 (3% behind, was 33%) and BEATS gzip by 23%. Does NOT yet beat zstd (remaining 3% is literal coding: zstd's tuned literal entropy vs Cubrim cube/geomix). HOLDOUT does NOT move (0.2390, MODE_LZ 0/6) and concatenated DIVERSE holdout unchanged (49106 > gzip) -- both lack the cross-block/duplicate structure the whole-file LZ needs. NOT tuned to holdout.
artefact documentation/ephemeral/research/h25e-holdout-bench.json
start H-25f dedicated literal coder for the LZ residue (target the H-25e remaining ~3% to zstd on long-range): try a tuned order-1 literal entropy coder vs the cube/geomix path, pick smaller per competitive rail 2026-06-24T00:22:53Z
built H-25f: MODE_LZ literal residue now coded by min of 3 candidates with a lit_kind byte -- 0=nested cube/BWT/rANS pipeline (strongest local model, pays cube header), 1=direct order-0 rANS (no cube framing), 2=direct order-1 rANS (zstd-style separate literal table); decoder dispatches on lit_kind; n_lits derived = n_tokens-n_matches
tests-green cargo build --release clean; cargo test GREEN lib 213 + integration 14, 0 failed; round-trip byte-exact; tuned corpus UNCHANGED (regression-proof: competitive rail + mode byte + lit_kind pick)
measured 120KB-MULTICOPY: 5359 -> 5335 (lit_kind=1 order-0 rANS narrowly beats cube by skipping the cube header on the random-18 residue) vs gzip 6950 vs zstd 5202 -- beats gzip 23%, gap to zstd 157B -> 133B (3.0% -> 2.5%); MARGINAL, does NOT close to zstd. The dedicated coder correctly picks lit_kind=0 (cube) on a structured/text residue and lit_kind=1 on a random residue.
measured HOLDOUT (run_holdout_bench, RT 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 (47638B) vs zstd 0.2214 (44701B) -- UNCHANGED, MODE_LZ 0/6 (holdout files have no cross-block structure, MODE_LZ never selected, so the literal-coder choice is irrelevant there)
VERDICT marginal H-25f: the dedicated literal coder is a real, regression-proof feature (picks order-0/order-1/cube per residue) but it does NOT close the last gap to zstd on long-range (120KB 5335 vs zstd 5202, still 2.5% behind). HONEST RE-DIAGNOSIS: the H-25e premise was partly wrong -- the residue was ALREADY coded near its order-0 entropy by the cube path, so a better literal coder only shaves ~24B (cube-header avoidance). The remaining gap is LZ FRAMING/SEQUENCE overhead (token_streams 210B + MODE_LZ/lit headers + per-token flag stream) vs zstd's combined-FSE sequence coding (literal-length/match-length/offset interleaved with shared state). Holdout does NOT move (0.2390). NOT tuned to holdout.
artefact documentation/ephemeral/research/h25f-holdout-bench.json
start H-25g combined SEQUENCE coder for MODE_LZ token streams (H-25f re-diagnosis target: remaining ~2.5% to zstd on long-range is FRAMING overhead -- 8 separate rANS streams each pay fixed table+state, dominating for few matches) 2026-06-24T00:43:09Z
built H-25g: serialize the whole token structure as zstd-style sequences -- per match (literal_length, match_length, offset_mode[, new_distance]) as varints + trailing literal run, into ONE buffer; code it with min(raw, order-0 rANS, order-1 rANS); DROPS the per-token flag stream entirely. Competitive seq_format byte (0=separate per-stream H-25f, 1=combined), pick smaller; decoder reconstructs interleaving from per-match literal-run-lengths
tests-green cargo build --release clean; cargo test GREEN lib 213 + integration 14, 0 failed; round-trip byte-exact; tuned corpus UNCHANGED (regression-proof: competitive rail + mode byte + seq_format + lit_kind picks)
measured 120KB-MULTICOPY: 5335 -> 5211 (seq_format=1 combined won, dropped ~124B of per-stream framing) vs gzip 6950 vs zstd 5202 -- now WITHIN 9 BYTES of zstd (0.17% behind, was 2.5%); ESSENTIALLY TIED with zstd-19 on long-range, beats gzip by 25%. RT PASS, mode=3.
measured HOLDOUT (run_holdout_bench, RT 6/6): Cubrim 0.2390 (48255B) vs gzip 0.2359 vs zstd 0.2214 -- UNCHANGED, MODE_LZ 0/6 (no cross-block structure; combined coder irrelevant there). CONCATENATED 202KB diverse: cubrim 49106 (mode=2 MODE_LZ falls back, no cross-file repeats) vs gzip 48342 -- UNCHANGED
VERDICT win-longrange H-25g: CLOSES the last gap to zstd on genuine long-range data -- 120KB 5211 vs zstd 5202 (0.17% behind, down from 2.5%), Cubrim now MATCHES zstd-19 on long-range/duplicate data and beats gzip by 25%, all regression-proof + round-trip byte-exact. The combined sequence coder removed the per-stream framing overhead (H-25f re-diagnosis confirmed: it WAS framing, not literals). HOLDOUT does NOT move (0.2390) and concatenated DIVERSE holdout unchanged -- those corpora simply lack the cross-block/duplicate structure that whole-file LZ exploits; their ceiling is the local BWT-family coder (~gzip parity, behind zstd). NOT tuned to holdout.
artefact documentation/ephemeral/research/h25g-holdout-bench.json
start H-25g-VERIFY harden H-25g for operator promotion (NO push/PR -- operator-gated like H-19): full suite + generalisation across real long-range shapes + zero-regression + holdout RT 2026-06-24T00:51:43Z
VERIFY-1 full test suite GREEN: 227 passed total (213 lib + integration 2+1+1+10), 0 failed, 0 ignored. cargo build --release clean.
VERIFY-2 generalisation across 3 REAL long-range corpora (NOT synthetic, NOT tuned), cubrim --value-scheme lz-rans vs gzip-9 vs zstd-19, RT byte-exact each:
  A srctree.tar (1505280B, tar of /usr/include/*.h -- cross-file boilerplate): cubrim 286447 vs gzip 320321 vs zstd 257542 -- BEATS gzip -11%, behind zstd +11%; MODE_LZ(3) selected; RT PASS
  B multiversion.bin (888351B, 3 git-historical versions of codec.rs concatenated -- near-duplicate): cubrim 61886 vs gzip 172553 vs zstd 56165 -- CRUSHES gzip -64%, within +10% of zstd; MODE_LZ(3); RT PASS
  C repeated.log (448761B, realistic syslog with repeated lines): cubrim 22041 vs gzip 37295 vs zstd 26970 -- BEATS BOTH (incl zstd -18%!) via the chunked BWT/geomix path (mode=2, NOT LZ); RT PASS
VERIFY-3 ZERO REGRESSION tuned 10-file corpus (run_bench --value-scheme bwt-rans): aggregate 0.158273 (= champion), per-file byte-identical to champion (sparse_clustered 169, text 1525, log_like 557, binary_mixed 5330, block_bound_runs 2389, dense/random_high/sparse_small/both_sparse raw 4109/4109/269/29/37), RT 10/10 PASS
VERIFY-4 HOLDOUT round-trip 6/6 PASS; aggregate Cubrim 0.2390 vs gzip 0.2359 vs zstd 0.2214 (unchanged, MODE_LZ 0/6 -- no cross-block structure)
VERDICT-VERIFY H-25g-VERIFY: zstd-parity is DATA-SHAPE-DEPENDENT, not uniform. Cubrim BEATS gzip on ALL long-range shapes; vs zstd-19: TIED on pure-duplicate (synthetic 12x-copy 5211 vs 5202, 0.17%), within ~10% on near-duplicate versions, ~11% behind on a mixed source tarball, and BEATS zstd on repetitive logs (via BWT chunked, -18%). Zero regression on the tuned champion (0.158273, byte-identical) and holdout RT 6/6 -- all regression-proof + byte-exact. Honest: H-25g matches/approaches zstd on duplicate-heavy long-range data and beats it on log-like data, but does NOT uniformly match zstd across all real long-range shapes. Safe for operator promotion of the bigfiles+LZ stack (zero regression guaranteed by the competitive min + mode byte). NOT tuned to any corpus. NOT pushed.
start H-25h beat-zstd-uniformly: diagnose why zstd still wins on srctree.tar (+11%) and pure-duplicate (+0.17%); lever = repeat-offset-aware cost-optimal match selection (zstd's repcode lever) 2026-06-24T01:12:35Z
diagnosed (debug breakdown) srctree.tar MODE_LZ: token 217652 of 286447 (76%) for 78248 matches; sub-streams dist_b0 72525 + b1 55816 + b2 14234 = 142575 OFFSETS (66% of token, ~16 bits/offset for 71931 DISTINCT offsets) + len_lo 48955 (22%). The cost is the OFFSET ENTROPY FLOOR for ~72K distinct cross-file offsets, i.e. driven by MATCH COUNT, not offset-reuse (only ~6300/78248 hit a repeat offset -- tarball cross-file matches are genuinely diverse)
built H-25h: replaced longest-match greedy with COST-OPTIMAL repeat-offset-aware selection -- at each position pick max-net-saving of {hash-chain longest match, matches at the 3 recent offsets}; a shorter repeat-offset match (off~3 bits) can out-save a longer new-offset match (off~16-26 bits); lazy-1 on net-saving. zstd's repcode lever.
tests-green cargo build --release clean; cargo test GREEN 227 passed (213 lib + 14 integration), 0 failed; round-trip byte-exact
measured H-25h long-range (cubrim vs gzip-9 vs zstd-19, RT PASS each): srctree.tar 286447->284369 vs gzip 320321 vs zstd 257542 (beats gzip 11%, +10.4% zstd); multiversion 61886->61734 vs gzip 172553 vs zstd 56165 (crushes gzip 64%, +9.9% zstd); repeated.log 22041 vs gzip 37295 vs zstd 26970 (BEATS zstd 18%, BWT chunked); 120KB 5211->5213 vs zstd 5202 (tied 0.21%)
measured ZERO-REGRESSION: tuned 10-file aggregate 0.158273 (cubrim_total 18523/117032, byte-identical to champion), RT 10/10. Holdout 0.2390 unchanged, RT 6/6.
VERDICT marginal H-25h: the repeat-offset-aware cost-optimal parse is the CORRECT algorithm (zstd's repcode lever) and improves real long-range data (srctree -0.7%, multiversion -0.2%), regression-proof + byte-exact -- BUT does NOT achieve uniform zstd-beat. The mixed-tarball gap (+10%) is FUNDAMENTAL: it is driven by MATCH COUNT (72K distinct cross-file offsets, ~16 bits each = ~143KB offset floor), and zstd wins there via its BTULTRA OPTIMAL PARSER (DP cost-minimisation + binary-tree match finder) that finds FEWER/LONGER matches -> fewer offsets to code. Deeper hash chains (tested 2048) gave -0.7% for 25s (too slow, match count barely dropped 77973->77044) -- confirming the lever is parse OPTIMALITY, not search depth. Net Cubrim picture vs zstd-19: BEATS on logs (-18%), TIES on pure-duplicate (0.21%), within ~10% on near-duplicate versions and mixed tarballs; BEATS gzip on ALL. Uniform zstd-beat requires a btultra-class optimal parser -- a multi-day algorithm, the named next lever (H-25i). NOT tuned to any corpus.
start H-25i btultra-class OPTIMAL PARSER: DP cost-minimisation over the match graph to find fewer/longer matches (the mixed-tarball +10% gap to zstd is the offset-entropy floor from too many short matches) 2026-06-24T01:35:48Z
built H-25i: forward DP cost[i]=min coded-bits to reach i; edges = literal (+lit_bits) and match of length L at best distance for each hash-chain frontier length (capped LZ_OPT_LEN_CAP=128 + full longest); backtrack the min-cost path. Cost model = principled log2 entropy (NOT corpus-tuned). Round-trip guaranteed by the exact encoder/decoder regardless of parse.
built H-25i COMPETITIVE PARSE (regression-proof): the optimal DP breaks repeat-offset structure on duplicate data (120KB regressed 5213->5501 optimal-only), so encode_lz_prepass builds a container with BOTH the fast greedy parse (preserves rep structure) AND the optimal DP, and returns the smaller (build_lz_container helper); value-scheme keeps the fast greedy (runs per-block in the rail)
tests-green cargo build --release clean; cargo test GREEN 227 passed (213 lib + 14 integration), 0 failed; round-trip byte-exact
measured H-25i long-range (cubrim vs zstd-19, RT PASS each; vs H-25h): srctree.tar 284369->270724 vs zstd 257542 = +5.1% (WAS +10.4%, gap HALVED, optimal-parse won); multiversion 61734->59772 vs zstd 56165 = +6.4% (WAS +9.9%, optimal won); repeated.log 22041 vs zstd 26970 = -18.3% (BEATS zstd, BWT chunked); 120KB 5213 vs zstd 5202 = +0.2% (TIED, greedy won via competitive pick -- NO regression). gzip beaten on ALL.
measured ZERO-REGRESSION: tuned 0.158273 (cubrim 18523/117032, byte-identical to champion), RT 10/10; holdout 0.2390 unchanged, RT 6/6
measured SPEED: optimal parse ~27s for 1.5MB (DP scans every position + double container build); decompression fast. Research max-ratio path; acceptable for big files, noted.
VERDICT progress-not-uniform H-25i: the optimal DP parser is the BIGGEST mixed-data gain yet -- it HALVED the gap to zstd (srctree +10.4%->+5.1%, multiversion +9.9%->+6.4%) by finding fewer/longer matches, regression-proof (competitive greedy/optimal pick, tuned byte-identical, holdout RT 6/6, 120KB restored). BUT does NOT beat zstd uniformly: still +5-6% behind on mixed source tarball and near-duplicate versions. Plain answer: does NOT beat zstd on the mixed tarball; narrows the gap from +10% to +5%. Net vs zstd-19: BEATS on logs (-18%), TIES on pure-duplicate, +5-6% on tarball/versions; BEATS gzip on ALL. The remaining ~5% is (a) the hash-chain match finder yields fewer match candidates than zstd's binary tree -- the optimal parse is only optimal over the candidates it sees; (b) the DP cost model is not repeat-offset-aware so it slightly mis-prices. Closing it needs a binary-tree/suffix-automaton match finder feeding the DP (H-25j) -- an even larger algorithm. NOT tuned to any corpus.
start H-25j-lite repeat-offset-aware DP cost model: make the H-25i optimal parser price matches at the 3 recent offsets cheaply (cause b of the residual zstd gap) 2026-06-24T10:31:51Z
tests-green H-25j-lite: cargo build --release clean; cargo test 227 passed (213 lib + 14 integration), 0 failed; rep-aware DP cost only changes the parse, RT guaranteed by exact encoder/decoder + competitive rail
measured H-25j-lite long-range (regenerated fixtures, RT PASS each, baseline=H-25i HEAD): repeated.log 13123->11889 (-9.4%, vs zstd +30.4%->+18.1%); multiversion 61480->61412 (-0.11%); srctree.tar 86288->86152 (-0.16%); multicopy120k 5016->5017 (+1B, parse artefact, untracked fixture). ZERO-REGRESSION: tuned 0.158273 byte-identical, RT 10/10; holdout 0.2390 byte-identical, RT 6/6
VERDICT marginal-win-longrange H-25j-lite: rep-aware DP cost model (cause b of the residual zstd gap) helps rep-offset-rich long-range (repeated.log -9.4%) and is regression-proof + byte-exact, but does NOT beat zstd uniformly -- the ~8% tarball/near-duplicate gap is dominated by cause (a) match-finder candidate density (open as H-25j-full, binary-tree finder). Leaderboard untouched. Committed on feat/cubr-bigfiles, NOT pushed.
FINAL H-25j-lite: commit 8881245 on feat/cubr-bigfiles (NOT pushed). cargo test 227 green, tuned 0.158273 byte-identical (zero regression), holdout 0.2390 byte-identical, RT clean. Marginal long-range win (repeated.log -9.4%), does NOT beat zstd uniformly. Next lever H-25j-full (binary-tree match finder, lever a). DONE.
start H-25j-full binary-tree match finder feeding the DP (lever a): LZMA-style BST over suffixes returns longest-at-each-distance-class candidates the hash chain misses; ADD to the rep-aware DP (superset of candidates, regression-proof vs H-25i) 2026-06-24T11:10:01Z
built H-25j-full: LZMA-style binary-tree match finder (bt_get_matches) feeding the rep-aware optimal DP as a SUPERSET of hash-chain candidates (never worse by cost model); new adversarial RT test
tests-green H-25j-full: cargo build --release clean; cargo test 228 passed (214 lib + 14 integration, 0 failed); new test_bt_match_finder_round_trips_adversarial green; round-trip byte-exact
measured H-25j-full long-range (RT PASS each, vs H-25j-lite): srctree.tar 86152->85398 (vs zstd +8.3%->+7.3%); multiversion 61412->61007 (+8.5%->+7.7%); repeated.log 11889->11774 (+18.1%->+17.0%); multicopy120k 5017 unchanged. ZERO-REGRESSION: tuned 0.158273 byte-identical RT 10/10, holdout 0.2390 byte-identical RT 6/6. ~73s/1.9MB (BT + chain double find)
VERDICT progress H-25j-full: the binary-tree finder surfaces longer/cleaner matches the hash chain misses, narrowing the zstd gap a further ~1% on mixed/near-duplicate data (srctree +7.3%, multiversion +7.7%), regression-proof + byte-exact. Does NOT beat zstd uniformly: ~7-8% remains. With match selection no longer the bottleneck, the residual floor is OFFSET CODING (zstd FSE offset-code + repcode vs Cubrim LEB128-varint distance through rANS). Next lever H-25k: FSE/rANS offset-code model. Leaderboard untouched. Committed on feat/cubr-bigfiles, NOT pushed.
FINAL H-25j-full: commit 01617c8 on feat/cubr-bigfiles (NOT pushed). cargo test 228 green, tuned 0.158273 byte-identical, holdout 0.2390 byte-identical, RT clean. Progress (zstd gap +8%->+7.3% srctree, +7.7% multiversion), does NOT beat zstd uniformly; residual floor is offset coding. Next lever H-25k (FSE offset-code model). DONE.
start H-25k FSE/rANS offset-code model (residual floor from H-25j-full): replace LEB128-varint new_distance through rANS with zstd-style offset-code (bit-length bucket entropy-coded + raw low bits); new seq_format behind competitive min (regression-proof) 2026-06-24T11:48:31Z
built H-25k: offset-code sequence coder (seq_format 2) -- new distances split into bit-length code (rANS) + raw low bits, behind competitive min over seq_formats {0,1,2}; new lz_encode/decode_token_offcode + lz_repcode_classify helper
tests-green H-25k: cargo build --release clean; cargo test 229 passed (215 lib + 14 integration, 0 failed); new test_offcode_token_coder_round_trips green; round-trip byte-exact
measured H-25k vs gzip-9 vs zstd-19 (RT PASS each):
  multicopy120k.bin 5017 -> 4448 vs zstd 3690 = +20.5% (was +36.0%, seq_format=2 offset-code WINS)
  multiversion.bin 61007 -> 61007 vs zstd 56625 = +7.7% (unchanged, seq_format=0 separate wins)
  srctree.tar 85398 -> 85398 vs zstd 79569 = +7.3% (unchanged, seq_format=0 separate wins)
  repeated.log 11774 -> 11774 vs zstd 10063 = +17.0% (unchanged, seq_format=0 separate wins)
measured ZERO-REGRESSION: tuned 0.158273 byte-identical (18523/117032), RT 10/10; holdout 0.2390 byte-identical, RT 6/6
VERDICT marginal-win H-25k: offset-code model WINS on pure-duplicate (multicopy -11.3%, gap to zstd +36%->+20.5%) but does NOT close the diverse-offset gap. KEY FINDING: on srctree/multiversion/repeated.log the EXISTING separate per-stream coder (seq_format 0, byte-split + order-1 rANS context) already beats both combined-varint (1) AND offset-code (2) -- Cubrim was ALREADY coding offsets competitively, so the H-25j-full "offset coding is the floor" diagnosis was half-right. Offset coding is NOT the residual lever. Regression-proof + byte-exact, leaderboard untouched. Next H-25l (parse/match-count + literal entropy, diminishing returns). Committed on feat/cubr-bigfiles, NOT pushed.
FINAL H-25k: commit edabfe0 on feat/cubr-bigfiles (NOT pushed). cargo test 229 green, tuned 0.158273 byte-identical, holdout 0.2390 byte-identical, RT clean. Marginal win on pure-duplicate (multicopy +20.5% vs zstd, was +36%); diverse-offset gap unchanged (separate-stream coder already competitive). Offset coding ruled OUT as the residual lever. DONE.
start H-25l parse/match-count + literal-entropy on residual zstd gap (mixed/near-duplicate ~7%, offset-coding ruled out H-25k); CHARGED DIAGNOSE FIRST 2026-06-24T12:08:23Z
built H-25l: DP offset-cost recalibration LZ_OFF_COST_SCALE=0.70 (was raw bit_length); cost-model only changes MODE_LZ optimal parse, RT guaranteed by exact encoder + competitive rail; instrumentation reverted
diagnosed (charged, Gotcha #6) srctree.tar MODE_LZ: token_block 81% of output, new-offset byte-split b0+b1+b2=119162 = 52% of WHOLE FILE for 64203 distinct offsets = 14.85 bits/offset (real coder) vs DP-modeled ~22 (raw 2+bit_length) -> DP OVERPRICES new offsets ~0.7x, under-takes short new-offset matches
swept scale {1.0,0.85,0.75,0.70,0.65,0.55}: 0.70 = robust min (srctree 229164->228989 -175, multiversion 64575->64195 -380, both improve, neither regresses); 0.65 overfits multiversion (-422) but REGRESSES srctree (+43) -> rejected as corpus-overfit; 0.70 matches measured coder efficiency 14.85/~20~=0.74 (coder property, not corpus knob)
tests-green cargo build+clippy(0 new) + test 215 lib + 14 integration GREEN 0 failed; RT byte-exact all fixtures
measured H-25l long-range (scale 0.70, RT PASS each): srctree.tar 228989 vs zstd 221518 = +3.37% (was +3.46%); multiversion.bin 64195 vs zstd 60978 = +5.27% (was +5.90%); multicopy120k 10056 unch (rep-dominated); repeated.log 56921 unch
measured ZERO-REGRESSION: tuned 0.158273 byte-identical (18523/117032, per-file = champion), RT 10/10; holdout 0.2390 byte-identical (48255), RT 6/6 (MODE_LZ 0/6, change irrelevant on <=64KB)
VERDICT marginal-win H-25l: offset cost-model was mis-calibrated (raw bit_length vs real ~0.7x); recalibrating to 0.70 shaves srctree -0.08% / multiversion -0.59% on genuine mixed/near-duplicate long-range, structurally regression-proof (parse-only + exact encoder + <=64KB byte-identical). Does NOT close zstd gap (still +3.4%/+5.3%): residual = offset entropy floor from ~64K distinct cross-file offsets (data-determined, not coding inefficiency). Deep diminishing-returns, as brief predicted. Committed on feat/cubr-bigfiles, NOT pushed.
artefact documentation/ephemeral/research/h25l-report.md
FINAL H-25l: commit 705b29f on feat/cubr-bigfiles (NOT pushed). cargo test 229 green, tuned 0.158273 byte-identical (zero regression), holdout 0.2390 byte-identical RT 6/6, RT clean. Marginal long-range win (srctree -0.08%, multiversion -0.59% vs zstd); offset entropy floor (~64K distinct cross-file offsets) is data-determined, NOT a coding lever. Parse/match-count + literal-entropy levers now BOTH near-exhausted on mixed data. DONE.
STOP clean: GO committed-not-pushed, deep diminishing returns; monitor/operator to assign next hypothesis or call the H-25 LZ line complete.
start H-26 CHANGE-OF-CLASS: pre-LZ transform to REDUCE distinct cross-file offsets BEFORE matcher (dedup / long-range reorder / dictionary-index) so the DATA-DETERMINED offset-entropy floor itself drops; CHARGED-DIAGNOSE FIRST, permutation/dict cost MANDATORY decoder branch (Gotcha #7) 2026-06-24T12:38:44Z
diagnosed H-26 (charged, Gotcha #6+#7): two pre-LZ offset-reducing transforms probed on srctree.tar + multiversion.bin
PROBE-1 generic reorder/grid-dictionary: codes match as source-bucket index; LOSES x1.5-2.0 once within-bucket precision (log2 W per match — decoder needs exact source byte) is charged (srctree W=4096 192686 vs bar 126716; multiversion 44138 vs 27368). Disorder relocates bucket-index<->within-bucket, does not vanish. NO-GO (Gotcha #7).
PROBE-2 CDC exact-dedup (position-invariant chunk-ids, the valid within-offset escape): offset-only charged model suggested WIN (multiversion 67% exact-dup mass, drift makes LZ offsets expensive but chunk-ids cheap) BUT end-to-end LOSES
E2E-DECISIVE: removing 67% of multiversion bytes (exact-dup mass) changed real cubrim output by only 201 B (64175->63974) — that mass was ALREADY near-free as LZ matches + rep-cache; dedup total 64841 (res 63974 + flags 728 + refs 139) WORSE than 64175. srctree S=1024 228947 ~tie (noise). The charged 'LZ pays 21566 B for dup mass' was a phantom (avg 13 b/match vs real 0.6 b/match).
measured H-26 (NO code shipped, codec = committed H-25l 705b29f): multiversion dedup 64841 vs zstd 60978 = +6.3% (WORSE than current +5.3%); srctree dedup 229295 vs zstd 221518 = +3.5% (= current). Both transforms NO-GO.
ZERO-REGRESSION trivially: codec.rs byte-identical to committed H-25l (no shipped change), so tuned 0.158273 + holdout 0.2390 byte-identical by construction; cargo build clean.
VERDICT nogo-class H-26: the ENTIRE pre-LZ offset-reducing transform class is closed. Offset-entropy floor is data-determined AND information-conserved — any transform reducing the COUNT of distinct cross-file offsets must re-transmit the removed info (within-bucket stream, or residual+chunk-ref stream), and LZ already codes that structure at its floor. Gotcha #7 generalised to the cross-file-offset domain, confirmed end-to-end (NOT just charged-model). The mixed/near-duplicate gap to zstd (+3.4%/+5.5%) is zstd's FSE-offset + btultra2 parse micro-efficiency, NOT a missing structural transform. Added Gotcha #8 to CLAUDE.md. Leaderboard untouched, NOT pushed.
artefacts documentation/ephemeral/research/{probe_h26_offset_floor.py, probe_h26_dedup_e2e.py, h26-report.md}; CLAUDE.md Gotcha #8
STOP clean: H-26 honest NO-GO-class (offset-reducing pre-transform class closed end-to-end); monitor/operator to assign next class or accept the zstd micro-efficiency gap as the residual.
start H-27 context-modelled offset coding (close residual zstd micro-efficiency gap +3.4%/+5.5%): condition the byte-split offset streams on recent state (prev byte / len-class) vs current context-free order-0; CHARGED-DIAGNOSE conditional-entropy gain FIRST (offcode+rawbits ruled out H-25k -- byte-split already beats it) 2026-06-24T13:52:40Z
diagnosed H-27 (charged, Gotcha #6): context-model the offset byte-split on recent state vs current context-free order-0; dumped winning MODE_LZ parse new-distance stream (srctree 70174, multiversion 18595 new dists)
PROBE ideal H1|prev-byte: srctree byte-split 131346->119535 (-9.0%), multiversion 33588->26483 (-21.2%) -- BOTH exceed gap to zstd (7471/3197 B) IN THE IDEAL
PROBE static order-1 rANS (codebase rans_order1_encode, full freq table per >=16-obs prev-byte context): near-uniform offset low-bytes => ~256 ctx x ~256 nonzero syms = +126KB(srctree b0)/+50KB(multiversion b0) tables, 10-20x the saving; competitive min picks order-0 everywhere => +0.0% DEAD
PROBE realistic ADAPTIVE order-1 range coder (no tables, KT online predictor = what scheme9/10/11 infra achieves): 256x256 cells but only 18K-70K symbols (~1 obs/cell) => learning cost EXCEEDS conditional-entropy saving; adaptive-o1 137204 vs adaptive-o0 131986 srctree (-4.5% WORSE), 35710 vs 34107 multiversion (-6.3% WORSE)
PROBE learnable low-cardinality contexts (bucket|prev-bucket, bucket|len-class) carry <=3% even ideal (35626->35180 -1.3%, ->34451 -3.3%) -- nothing once learning charged; delta-coding +0.0% (offsets jump between inter-version clusters, not monotonic)
measured (NO code shipped, codec=committed H-25l 705b29f): srctree.tar 1310720 -> 228989 vs zstd 221518 = +3.4% (gzip 274924, beats -17%); multiversion.bin 999210 -> 64175 vs zstd 60978 = +5.2% (gzip 198710, beats -68%)
ZERO-REGRESSION trivially: codec.rs byte-identical to committed H-25l, tuned 0.158273 + holdout 0.2390 byte-identical by construction
VERDICT nogo H-27 (no Rust): the ideal conditional-entropy gain (-9%/-21%) is a LEARNING-COST MIRAGE -- the only context with meaningful gain (prev-byte) is UNLEARNABLE at these stream sizes (256x256 cells, ~1 obs/cell => real adaptive o1 LOSES to o0 by 4.5-6.3%); static-table variant deader (+50-126KB); learnable low-cardinality contexts carry <=3%. zstd does not beat us via order-1-on-offsets either (repcodes+single adaptive FSE offcode = Cubrim rep-cache + byte-split, already competitive per H-25k). Residual zstd gap NOT recoverable by offset-code context modelling. Added Gotcha #9 (H1 probe is asymptotic; charge a real online predictor + cell/stream sanity). Leaderboard untouched, NOT pushed.
artefacts documentation/ephemeral/research/{probe_h27_ctx_offset.py, h27-report.md}; CLAUDE.md Gotcha #9
STOP clean: H-27 honest NO-GO (context-offset-coding closed via real-adaptive-predictor charge); monitor/operator to assign next class or accept zstd micro-efficiency gap as residual.
start H-28 CHANGE-OF-CLASS attack LITERALS (out of LZ/offset micro): charged literal-vs-match breakdown on mixed tarball + adaptive PPM/context-mix literal model with online-learning charge (Gotcha #9) 2026-06-24T14:30:00Z
diagnosed H-28 (charged, env-gated dump in build_lz_container then fully reverted; fixtures regenerated srctree.tar 1310720 + multiversion.bin 1021595): winning optimal-parse MODE_LZ container -- srctree literal_blob 32382 (14.0% of 231433) vs match/token 199027 (86.0%), n_lits 42000; multiversion literal_blob 11982 (18.4% of 65277) vs token 53271 (81.6%), n_lits 15604; BOTH already coded lit_kind=1 order-0 rANS (nested BWT+geomix context-mix AND order-1 LOST in the live rail)
probe H-28 (probe_h28_literal_model.py, charged real online KT predictor + ideal H_k + cell/stream sanity per Gotcha #9): ideal H3 collapses (srctree 3356 -90% vs H0 32061; multiversion 899 vs 11623) BUT real adaptive o2/o3 are +15-28% WORSE than o0 (srctree o2 39328 o3 41490; multiversion o2 14932 o3 15543) -- 0.005-0.029 obs/cell, unlearnable; best charged real = srctree adaptive-o1 32069 (save 313B/1.0%, closes 3.2% zstd gap), multiversion adaptive-o0 11750 (save 232B/1.9%, closes 6.0%), nearly all just avoided order-0 table
measured (NO code shipped, codec byte-identical to H-27 1bca7c9): srctree.tar 231433 vs zstd 221518 (+4.48%) gzip 274924 (beats -15.8%); multiversion.bin 65277 vs zstd 61406 (+6.30%) gzip 204141 (beats -68%); RT PASS both
ZERO-REGRESSION trivially: codec.rs git-diff empty (instrumentation reverted), tuned 0.158273 + holdout 0.2390 byte-identical by construction; cargo test 229 GREEN (215 lib + 14 integration, 0 failed)
VERDICT nogo H-28 (literal floor data-determined): the literal residue is (a) only 14-18% of output, (b) already within ~1% of order-0 entropy (32382 ~ H0 32061 + table), (c) un-improvable by higher order -- post-optimal-parse residue is high-entropy, streams 100-1000x too short for order-2+ (0.005-0.03 obs/cell, real adaptive o2/o3 LOSE), (d) coded order-0 by zstd too (Huffman literals, FSE only for sequences). Context-mixing already tried+lost (nested BWT+geomix). Residual zstd gap (+4.5%/+6.3%) NOT recoverable via literal coder -- it is the 82-86% match block offset-entropy floor + zstd FSE/parse micro-efficiency. Honest ceiling. Added Gotcha #10. Leaderboard untouched, NOT pushed.
artefacts documentation/ephemeral/research/{probe_h28_literal_model.py, h28-report.md}; CLAUDE.md Gotcha #10
start SCOUTING (analysis-only, NO Rust): is there an untried class beyond LZ with real potential, or is +4.5%/+6.3% the honest final ceiling? operator decision 2026-06-24T15:10:00Z
PROPOSAL-A FSE/btultra2 sequence micro-efficiency = MARGINAL: gap lives in match/token block (86%/82% of output); offset-entropy itself is data-determined floor (~64K distinct offsets x ~14.85 bits, both Cubrim+zstd pay it, H-26/27). Extractable lever = zstd btultra2 ITERATIVE price-feedback parse (re-run DP with updated FSE costs -> fewer/longer matches); H-25i/j single-pass DP already halved the gap (+10.4%->+4.5%), deeper search gave -0.7%/25s (diminishing). Ceiling estimate ~1-3% of match block = narrow +4.5%->~+2-3%, never structural, multi-day; IS the micro-efficiency class operator already accepts. Does NOT beat zstd on near-duplicate.
PROPOSAL-B non-LZ model (PPM*/CM/neuro/grammar Re-Pair/Sequitur) = MARGINAL-to-DEAD as a Cubrim increment: the two trailing fixtures are LZ-DOMINATED (near-duplicate codec.rs versions + cross-file boilerplate) -- duplication is LZ's home, a non-LZ model adds NO information source LZ misses (Gotcha #8 info-conservation: grammar/CM still must encode WHERE repeats are). BWT+geomix already IS an order-2/1/0 context-mixer and covers the statistical-local lever (loses on the high-entropy LZ residue per H-28). Only a paq/cmix-class WHOLE-FILE CM beats zstd on plain text -- but that is a different product at 100-1000x slowdown, a full architecture replacement not an entropy-coder swap, and STILL loses to LZ on the near-duplicate fixture. No structural headroom here.
PROPOSAL-C specialize for repetitive structured data (logs/telemetry/columnar) = HIGH within-class, the one honest GO: Cubrim ALREADY BEATS zstd-19 on repeated.log -18% (22041 vs 26970) via the BWT+geomix chunked path -- a GENUINE structural edge (BWT+context-mixing) that zstd LZ+FSE lacks, NOT micro-efficiency; also beats gzip everywhere. Reframe goal from general zstd-parity (data-determined ceiling) to specialist that BEATS zstd on its class. Ceiling: high+generalizing within log/telemetry/record-repetitive text; scope change not a general-data ratio breakthrough.
PROPOSAL-RECOMMEND: STOP chasing the general-purpose zstd gap (+4.5%/+6.3% is a data-determined micro-efficiency ceiling, confirmed by 5 converging NO-GOs H-25l/26/27/28). If operator wants a real GO it is CLASS-C specialization (characterize+harden+benchmark the log/telemetry/columnar class where BWT+geomix already beats zstd). A=marginal multi-day micro-grind, B=dead-to-marginal increment. Net standing: BEATS gzip universally, BEATS zstd on repetitive logs, ~gzip-parity-behind on general/mixed, never below gzip. Honest final answer: general-purpose potential is EXHAUSTED at micro-efficiency; structural upside remains ONLY in specialization.
MODE-CHANGE (operator 2026-06-24): PERMANENT continuous-improvement race UNTIL beating BOTH gzip-9 AND zstd-19; data-determined ceiling is NOT a stop condition; after every NO-GO propose next class/external idea, never choose STOP until leaders are beaten.
start H-29 (GO class-C) specialize logs/telemetry/columnar where Cubrim structurally beats zstd-19 (repeated.log -18% via BWT+geomix); steps: characterize class -> collect REAL representative corpus -> harden BWT+geomix path -> RELIABLY beat zstd-19 across WHOLE class (not one file). External research dispatched in parallel for H-30+. 2026-06-24T15:40:00Z
characterized H-29 class: built REAL 9-file class corpus (gen_class_corpus.sh, host-derived, disjoint from tuned); measured --value-scheme bwt-rans RT PASS all -- aggregate cubrim 219107 vs gzip 307388 (-28.7%, wins 8/9) vs zstd 219039 (+0.03% tie, wins ONLY 3/9: app_orchestrate -14.1%, forex_tick -4.2%, forex_usdchf -0.5%); repeated.log -18% was a specific case NOT the class
probe H-29 columnar (probe_h29_columnar.py, faithful real-codec on transformed bytes, 16B header charged, info-conservation-safe): reversible column-major field-split -- forex_tick 58741->44359 (zstd -4.2%->-27.7%), forex_usdchf 55274->38457 (-0.5%->-30.8%), status_timeseries 22889->20710 (+7.1% LOSS->-3.1% WIN flipped), deals_record 3799->3702 (+7.0%->+4.3% not flipped, tiny string-heavy)
research H-29 (agent afa18d32, SOTA log/columnar synthesis): top levers H-30 columnar-split (CONFIRMED by probe), H-31 timestamp-delta, H-36 CLP-template-split (high-ceiling, spike-gated); marginal H-32 XOR-cache/H-33 STC-digits/H-34 APM-SSE/H-35 zstd-dict. Logged H-30..H-36 to hypothesis-log.md for /evolution (session B publishes)
ZERO-REGRESSION: codec.rs byte-identical (research+probe round, no shipped code), tuned 0.158273 + holdout 0.2390 byte-identical by construction; cargo unchanged
VERDICT go H-29 (probe-confirmed, implementation queued): columnar field-split is the class-C hardening lever -- realizes -27..-31% vs zstd on numeric telemetry CSV + flips status_timeseries to a win, regression-proof by construction (competitive min(base,columnar)+mode byte). Class is NOT a ceiling. NEXT ROUND: implement MODE_COLUMNAR container (byte-exact reversible field-split, per-row field-count side stream, competitive rail + RT/property tests, tuned+holdout byte-identical). Continuous mode: after impl, stack H-31 delta then H-36 log-template. Leaderboard untouched, NOT pushed.
artefacts code/bench/gen_class_corpus.sh + documentation/ephemeral/research/{probe_h29_columnar.py, h29-report.md}; consilium/hypothesis-log.md H-29 + H-30..H-36
start H-29-IMPL MODE_COLUMNAR (byte-exact reversible columnar field-split, container mode 4, competitive rail, gated >64KB) 2026-06-25T09:30:00Z
built H-29-IMPL: MODE_COLUMNAR=4 (header.rs) + build_columnar_blob/encode_columnar/decode_columnar + read_u32 helper (codec.rs); split rows by \n + fields by detected delim (tries , tab ; |, keeps smallest), column-major emit, per-row field-count LEB128 side stream, both streams nested encode_base; wired into encode_with_config as min(base,lz,columnar) GATED on data.len()>cube_size_limit(64KB) + tabular detector (>=16 rows,>=2 cols,>=90% modal field-count -> excludes prose/JSON-lines)
tests-green H-29-IMPL: +5 columnar tests (round-trips-and-shrinks, ragged/edge-cases incl no-trailing-nl/empty-fields/TSV, not-selected-on-non-tabular prose, property 20 random tables, truncated-no-panic); full suite 234 GREEN (220 lib + 14 integration, 0 failed); clippy 0 new warnings; cargo build --release clean
measured ZERO-REGRESSION (critical invariant): tuned 10-file 0.158273 BYTE-IDENTICAL (18523/117032, per-file = champion, RT 10/10); holdout 0.2390 BYTE-IDENTICAL (48255, RT 6/6) -- config.json 66KB attempted columnar but tabular-gate rejected JSON -> fallback byte-identical, data.csv 17KB<64KB untouched by size-gate
measured CLASS (cubrim --value-scheme bwt-rans, RT PASS all 9): forex_tick 58741->44397 (zstd -4.2%->-27.6% WIN), forex_usdchf 55274->38514 (-0.5%->-30.7% WIN), status_timeseries 22889->20769 (+7.1%->-2.9% WIN flipped); AGGREGATE cubrim 219107->185883 vs zstd 219039 = +0.03%->-15.1% (now BEATS zstd), vs gzip 307388 = -39.5%; zstd-wins 3/9->4/9
VERDICT go H-29-IMPL MODE_COLUMNAR: columnar field-split flips the CSV/telemetry sub-class to CRUSH zstd by 27-31% and moves the WHOLE class aggregate from a zstd tie to a decisive -15.1% zstd WIN; regression-proof by construction (competitive min + mode byte + >64KB gate), tuned+holdout byte-identical, RT byte-exact, 234 tests green. NOT a ceiling -- remaining losses are LOG files (journal/toolchain/dpkg) + 2 tiny files. NEXT (continuous-mode): H-31 timestamp/monotonic-column delta (stacks on columnar), then H-36 CLP-log-template split. Leaderboard untouched, NOT pushed.
artefacts: header.rs MODE_COLUMNAR + codec.rs columnar impl + 5 tests; documentation/ephemeral/research/h29-report.md IMPLEMENTATION section
start H-31 monotonic-column first-order delta (stacks on H-30 columnar, inside MODE_COLUMNAR) 2026-06-25T10:30:00Z
probe H-31 (probe_h31_delta.py, faithful real-codec): delta on canonical monotone-int columns -- forex_tick col 44343->delta 36768 (-17.1%), forex_usdchf 38441->31127 (-19.0%), status 20694->20312; journal.log/dpkg.log NOT TABULAR (columnar/H-31 cannot apply -- variable field counts, logs need H-36)
built H-31: columnar_delta_encode/decode + canonical_i64 (v.to_string()==cell, rejects leading-zeros/+/floats) + non-decreasing gate; per-column colmodes byte + ends_nl flag (strip trailing-newline empty row that poisoned column-0 detection) added to MODE_COLUMNAR wire; reversible prefix-sum; competitive by construction (whole columnar blob still min(base,lz,columnar), gated >64KB+tabular)
tests-green H-31: +2 (delta canonical/monotonic unit incl leading-zero/non-monotone/text-stay-raw, shrink-on-monotonic-csv); full suite 236 GREEN (222 lib + 14 integration, 0 failed); clippy 0 new warnings; cargo build clean
measured ZERO-REGRESSION: tuned 0.158273 BYTE-IDENTICAL (RT 10/10), holdout 0.2390 BYTE-IDENTICAL (RT 6/6) -- wire change only affects MODE_COLUMNAR (>64KB+tabular gate), all tuned/holdout files untouched
measured CLASS (cubrim --value-scheme bwt-rans, RT PASS all): forex_tick 44397->36846 (zstd -27.6%->-39.9% WIN), forex_usdchf 38514->31207 (-30.7%->-43.8% WIN), status 20769->20398 (-2.9%->-4.6% WIN); AGGREGATE cubrim 185883->170654 vs zstd 219039 = -15.1%->-22.1% (beats zstd harder), vs gzip 307388 = -44.5%; zstd-wins 4/9
VERDICT go H-31 monotonic-column delta: deepens the telemetry sub-class from -15% to -22% below zstd (forex -40/-44%), regression-proof+byte-exact, tuned/holdout byte-identical, 236 tests green. HONEST SCOPE: H-31 only acts inside MODE_COLUMNAR (uniform tables) -- it does NOT flip the LOG files (journal/toolchain/dpkg are not column-uniform, columnar never engages); operator premise 'H-31 finishes the logs' corrected -- it finishes the TELEMETRY half. NEXT (continuous): H-36 CLP-style log-template split for the logs. Class NOT a ceiling. Leaderboard untouched, NOT pushed.
artefacts: codec.rs columnar_delta + canonical_i64 + ends_nl wire + 2 tests; probe_h31_delta.py; consilium/reports/H-31-columnar-monotonic-delta.md
