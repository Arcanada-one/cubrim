# Vendored from dr-orchestrate

These consilium scripts are vendored copies of the dr-orchestrate plugin
(source: Datarim framework repo, plugins/dr-orchestrate/scripts/). They are
NOT installable as a runtime plugin (non-standard layout), so the cluster
vendors them here and invokes them by path. Re-sync from the source repo when
the upstream scripts change.
