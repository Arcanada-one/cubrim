# Build output and fuzzer-generated data. The targets and their documentation
# are source; the corpus is regenerated by `cargo run --example gen_fuzz_seeds`
# and grows during a run, so it is not tracked.
target/
corpus/
artifacts/
coverage/
