CUBR-SPEEDFLOOR-20260811 — measurement provenance

preregistration : documentation/ephemeral/research/CUBR-SPEEDFLOOR-20260811.md
  merged to main as da16b05 at 2026-08-11T17:05:13Z; measurement began 17:05:27Z.
  Prediction preceded measurement.

host       : arcana-devs, 16 cores, 125 GiB RAM. SHARED with ~40 other agent sessions;
             load1 ranged 9.9-31.2 across the run (loadlog.txt). This is why the
             interleaved pass exists — see the report's timing-honesty section.
pin        : taskset -c 0-15 on every compress and decode. Pin NOT widened.
memory cap : systemd-run --user --scope -p MemoryMax=64G -p MemorySwapMax=0.
             The first attempt used 8G (inherited from the zero-rep slice runs) and the
             cgroup OOM-killed cubrim compress on dickens. Cause: tbits =
             clamp(ceil(log2 len)+3, 18, 27); a 2 MB slice gets tbits=24, full dickens
             gets tbits=27 — 8x the table. Measured: compress peak ~11.1 GB,
             decode RSS 10.5 GiB. The cap contained the kill to its own scope.

binary     : built from the G2 attribution's OWN frozen source commit
             3a13f486aea51470e2079ba66abb94d99fd782d9, so the landed per-file Amdahl
             bounds apply to this binary rather than being mixed across commits.
  built sha256    : 8947ea9b155c10d33316cba60647ff4d755e7a6a3ceff3c75a9b5b3e01fa8d9c
  attribution sha : d4b9fc85a242f887fb1a49bd849c35779c48b8fda04480969309f2d0bb0211cb
  cargo           : cargo 1.97.1 (c980f4866 2026-06-30)
  SAME SOURCE, DIFFERENT BINARY BYTES. Independently reproduces the blocker that
  terminated NEW-24 G6: code/cubrim-rs/Cargo.lock is gitignored and dependencies are
  unpinned semver ranges, so two builds of one commit are not bit-identical.
  BEHAVIOUR is identical, though — see the report's binary-equivalence check.

corpus     : /home/dev/cubr-cubecore-research/corpus-silesia
  dickens 10,192,446 B ; x-ray 8,474,240 B  (canonical Silesia)

competitors: xz 5.4.5 -9 | zstd 1.5.5 -19 | brotli 1.1.0 -q11 | gzip 1.12 -9
             bzip2 1.0.8 -9 | lz4 1.9.4 -12   — all run on THIS host, same pin,
             not read from a cross-meta table.

gates      : every decode verified by cmp AND sha256 against the original BEFORE its
             timing row is written; a VOID gate aborts the cell. 42 gated observations
             in the main pass + 21 in the interleaved pass = 63, all OK, 0 VOID.

files      : results.tsv (main pass), interleaved.tsv (same-window pass), gates.tsv,
             loadlog.txt, run.sh, interleave.sh, analyze.py, SHA256SUMS
regenerate : python3 analyze.py   — every table in the report, no hand-typed arithmetic
