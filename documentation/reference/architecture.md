# Reference: Architecture / System Map

> Stub (Diátaxis: reference).
>
> **🔒 Algorithm internals are confidential** and specified privately (see
> `consilium/` rulebook + `datarim/prd/`). They are NOT documented here. This
> public-facing reference is populated only with the **non-secret** implementation
> map (CLI surface, file I/O, build layout) once code exists — never the
> compression mechanism.

## System map (public surface — populated after code exists)

- [TODO: CLI commands + flags]
- [TODO: input/output file handling (non-secret)]
- [TODO: build & crate layout]

> Mechanism, math, and encoding scheme live only in internal artefacts. Do not
> add them here — see `../../CLAUDE.md` § Secrecy Constraint.
