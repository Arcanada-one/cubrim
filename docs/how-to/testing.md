# How-to: Testing

> Stub (Diátaxis: how-to). Populate once `code/` is bootstrapped.

Headline check for this project is **lossless round-trip**: compress → decompress → byte-identical to input. Ratio is reported only after round-trip passes.

- [TODO: how to run the test suite]
- [TODO: how to add a property test for round-trip fidelity]
